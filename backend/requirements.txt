# Floor pins (no exact pins) so pip can resolve wheels for the local Python
# version. After a working install, freeze with:  pip freeze > requirements.lock
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.7
SQLAlchemy[asyncio]>=2.0.38
aiosqlite>=0.20
python-binance>=1.0.28
pandas>=2.2.3
numpy>=2.1
httpx>=0.27
pytest>=8.3
